<h1>Mes Favoris</h1>
<% if @favorites.any? %>
<div class="d-flex justify-content-evenly flex-wrap row-gap-2">

  <% @favorites.each do |favorite| %>
    <%= render partial: "shared/card1", locals: { meme: favorite.meme } %>
  <% end %>
  </div>
<% else %>
  <p>Vous n'avez aucun favori pour le moment.</p>
<% end %>
